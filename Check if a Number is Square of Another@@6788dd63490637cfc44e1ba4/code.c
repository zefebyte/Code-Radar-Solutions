#include <stdio.h>

int main() {
    int a, b;
    scanf("%d %d", &a, &b);

    // Check if b is equal to a * a
    if (b == a * a) {
        printf("Yes");
    } else {
        printf("No");
    }

  
}
